package eapli.ecafeteria.domain.users;

/**
 * Created by Nuno Bettencourt [NMB] on 24/03/16.
 */
public class AuthenticationServiceTest {
    /*
     * @Test(expected = InvalidUserException.class) public void
     * ensureAuthenticationIsNotPerformedWithEmptyDatabase() throws Exception {
     * Username userName = new Username("admin"); Password password = new
     * Password("admin"); AuthenticationService authenticationService = new
     * AuthenticationService(); authenticationService.authenticate(userName,
     * password); assertTrue(true); }
     */
}